[START]
   │
   ├─ "привет" / "hi" ───→ вывод справки ────────┐
   │                                               │
   ├─ "задание 1" ──────→ [TASK1_AWAITING_ARRAYS] │
   │                          │                    │
   │                          └─ "массивы [..] и [..]" → вычисление → [START]
   │
   ├─ "задание 4" ──────→ [TASK4_AWAITING_NUM1] → [TASK4_AWAITING_NUM2] → [TASK4_AWAITING_OP]
   │                          │                      │                       │
   │                          └─ число1 [...]        └─ число2 [...]        └─ "+" или "-" → результат → [START]
   │
   ├─ "задание 5" ──────→ [TASK5_AWAITING_ARRAY] → [TASK5_AWAITING_TARGET]
   │                          │                         │
   │                          └─ "массив [...]"         └─ "сумма N" → подсчёт → [START]
   │
   └─ "пока" / "exit" ──→ завершение программы